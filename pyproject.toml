[tool.poetry]
name = "avcicms"
version = "0.1.0"
description = "AVCI CMS Python SDK"
authors = ["AVCI CMS <info@avcicms.com>"]
readme = "README.md"
packages = [{include = "avcicms"}]

[tool.poetry.dependencies]
python = "^3.8"
httpx = "^0.27.0"
pydantic = "^2.6.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
